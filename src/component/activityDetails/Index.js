import styled from '@emotion/styled'
import React from 'react'

const StyledDiv = styled('div')({
    width: '360px',
    backgroundColor: '#ffffff'
})
const ActivitySection = () => {
  return (
    <StyledDiv>Todays Highlight</StyledDiv>
  )
}

export default ActivitySection;